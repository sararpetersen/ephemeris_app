import type { CSSProperties } from "react";

export function revealClass(_inView: boolean, _animation = "fadeInUp") {
  return "";
}

export function revealStyle(_index = 0, _staggerMs = 90): CSSProperties {
  return {};
}
